# 需要検証用LP 仕様書（v1）

## 1. LP目的

広告・Instagram等から初めて訪れた専門工事会社の担当者が、

> これは自分たちの提出書類業務の問題だ

と理解し、

> [3分で体験する]

を押してセルフガイドデモ（`index.html?demo=1`）へ進むこと。

このLPのKPIは「最後まで読んだ人数」ではない。初見ユーザーが自分の業務課題だと感じ、
3分デモを開始することが唯一の目的であり、LPだけで全てを説明しようとしない。
本格的なサービスサイトを作ることも目的ではない。

## 2. 対象ユーザー

- 専門工事会社（電気工事・空調・配管・足場・塗装・防水・内装・解体など）
- 社員・作業員規模10〜50名前後、複数の元請・複数の現場を抱えている会社
- 想定読み手：事務担当／安全書類担当／現場管理者／経営者
- 流入経路：広告・Instagram等（初回接触。製品を一切知らない前提）

## 3. 主要訴求

「AIがすごい」「DX」「次世代」を売らない。売るのは以下の業務改善のみ。

> 元請ごとに違う提出書類。もう、毎回ゼロから準備しない。

刺さる課題（LP内「こんな状態になっていませんか？」で提示）：

- 元請ごとに提出書類が違う
- 現場が増えるほど管理が複雑になる
- 資格証をLINEやメールで集めている
- 誰の何が不足しているか確認が大変
- 同じ会社情報・作業員情報を何度も入力する
- Excel／PDF／GreenSite／Buildeeなど提出方法がバラバラ

差別化の軸（「なぜ便利なのか」セクションで最重視）：
「元請ごとに別々に管理する」から「自社の情報を一元管理し、提出先に合わせて準備する」への変化。

## 4. セクション構成（`lp.html` 上から順）

| # | セクション | 目的 |
|---|---|---|
| 1 | ヘッダー | 製品名の掲示、常時「3分デモ」CTAへの導線 |
| 2 | ファーストビュー | 「誰向け」「何が楽になる」「何を押せばよい」を1画面で理解させる。PC 2カラム（コピー＋CTA／実UIの静的モック）、スマホは縦積み |
| 3 | こんな状態になっていませんか？ | 4つの課題カードで自分ごと化させる |
| 4 | このシステムなら | セルフデモと同じ順序の6ステップフロー＋CTA |
| 5 | 実際の画面 | A（不足確認）／B（資料依頼）／C（提出準備）の3場面を実UIの静的再構築で提示 |
| 6 | なぜ便利なのか | 「元請ごとに別々」→「自社で一元管理」の差別化を明示 |
| 7 | 料金仮説 | Standardプラン1つのみ。需要検証中の予定価格である旨を明示＋CTA |
| 8 | 対象外 | 総合施工管理システムではなく「提出書類の準備」に絞ったサービスであることを短く明示 |
| 9 | FAQ | 5問（GreenSite/Buildeeとの関係、作業員のアカウント要否、Excel運用との関係、正式版の有無、営業連絡の有無） |
| 10 | 最終CTA | ページ末尾でもう一度3分デモへ |
| 11 | フッター | 需要検証中である旨の注意書き |
| - | スマホ固定CTA | 600px以下で画面下部に常時表示（本文・FAQを大きく隠さない高さ） |

## 5. CTA一覧・遷移先

CTA文言はすべて「3分デモ」に統一。「資料請求」「お問い合わせ」「相談する」「無料登録」「会員登録」は
メイン導線として使用しない。

| 位置 | 文言 | `data-lp-event` | 遷移先 |
|---|---|---|---|
| ヘッダー | 3分デモ | `hero_demo_click` | `index.html?demo=1&source=lp#/dashboard` |
| ファーストビュー | 3分で無料デモを体験する | `hero_demo_click` | 同上 |
| このシステムなら（説明後） | 3分デモでこの流れを試してみる | `middle_demo_click` | 同上 |
| 料金仮説 | 月4,980円のサービスを3分で体験する | `price_demo_click` | 同上 |
| 最終CTA | 3分デモを体験する | `final_demo_click` | 同上 |
| スマホ固定CTA | 3分で無料デモ | `hero_demo_click` | 同上 |

すべて相対パス（`index.html?...`）でリンクしており、GitHub Pagesのリポジトリパスが変わっても
壊れにくい。`source=lp` は流入元計測用の追加パラメータで、`Demo.checkEntry()` は `demo` パラメータ
のみを見て判定するため、既存のデモ起動ロジックへの影響はない。LP独自の別デモ画面は作らず、
既存のセルフガイドデモ（ウェルカム画面→6ステップ）にそのまま合流する。

## 6. イベント一覧

`js/lp.js` の `recordLpEvent(event, meta)` が `teishutsu_lp_events_v1` に
`{ event, timestamp, ...meta }` を追記する。

| イベント名 | 発生タイミング |
|---|---|
| `lp_view` | `lp.html` 読み込み時に1回 |
| `hero_demo_click` | ヘッダー／ファーストビュー／スマホ固定CTAのいずれかを押した |
| `middle_demo_click` | 「このシステムなら」セクション後のCTAを押した |
| `price_demo_click` | 料金セクションのCTAを押した |
| `final_demo_click` | 最終CTAを押した |

## 7. 禁止表現

- 根拠のない数字（「作業時間90%削減」「年間○万円削減」「導入○社」「利用者満足度○%」「業界No.1」等）
- 架空のレビュー・導入事例（「○○設備株式会社様」「導入して残業が減りました」等）
- GreenSite／Buildeeについて「自動連携」「自動提出」「置き換える」という表現
  （現時点のコンセプトは「提出前の準備を効率化するサービス」であり、各サービスへの提出前の情報整理・
  不足確認・資料準備をまとめて管理するもの、という表現にとどめる）
- 本番提供済みと誤解させる表現（ページ内に「現在、正式版提供前の需要検証を行っています。掲載している
  画面・機能・料金は予定内容を含みます。」の注意書きを設置済み）

## 8. 将来のAnalytics接続ポイント

現時点では外部Analytics（GA等）は導入していない。`js/lp.js` の `recordLpEvent()` は
`js/demo.js` の `recordDemoEvent()` と同じ設計方針（`{ event, timestamp, ...meta }` を
`localStorage` に集約）を採っており、将来外部送信APIへ置き換える際は `recordLpEvent()` の
`localStorage.setItem()` 呼び出し箇所に送信処理を追加するだけで済む。`source=lp` パラメータは
LP経由の流入をセルフデモ側のイベントログ（`teishutsu_demo_events_v1`）と将来突き合わせる際の
手がかりとして温存している（現時点では `Demo` 側での参照・保存は行っていない）。

## 9. 変更しないもの

以下は既存のモック・セルフガイドデモと完全に共通で、LP追加によって変更していない。

- `index.html` および既存SPAルーティング
- `js/store.js` の状態管理・業務ロジック
- セルフガイドデモの6ステップ（`js/demo.js`）
- `mobile-submit.html`（作業員側提出画面）
- 通常モード、既存シナリオA〜D
