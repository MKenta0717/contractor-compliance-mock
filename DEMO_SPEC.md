# セルフガイドデモ 仕様書（v1）

## 1. 目的

営業担当・説明担当が介在しなくても、初めて訪れた専門工事会社のユーザーが
約3分でこのサービスの価値を理解できる「セルフガイドデモ」を提供する。

このデモは機能説明ツアーではない。ゴールは、ユーザー自身に以下の成功体験をさせることである。

```
現場ごとに必要書類が違う
  ↓
不足資料がすぐ分かる
  ↓
作業員本人へ資料提出を依頼できる
  ↓
スマホから資格証が届く
  ↓
事務担当が確認・登録する
  ↓
不足が解消される
  ↓
提出資料を準備できる
```

最終的に、初見のユーザーが「なるほど、このシステムがあると安全書類の準備が楽になるのか」と
説明なしで理解できる状態を目指す。

## 2. 対象ユーザー

- 専門工事会社（電気工事・空調・配管・足場・塗装・防水・内装・設備工事など）の事務担当者・現場管理者
- LP・Instagram広告等から初めてアクセスする、製品を一切知らないユーザー
- PC・スマートフォンの両方（特にInstagram経由はスマホが主戦場のため、375px幅を最重要とする）

## 3. 全体構成

- **通常モード**：既存のクリック体験モック。デモは強制しない。
- **セルフデモモード**：`Demo`（`js/demo.js`）が管理する、ガイド付きの6ステップ体験。

### 入口

| 条件 | 挙動 |
|---|---|
| 初回アクセス（`teishutsu_demo_seen_v1` 未設定） | ウェルカム画面を自動表示 |
| `?demo=1` 付きURL | 既に見たことがあっても毎回ウェルカム画面を表示 |
| 上記以外 | ウェルカム画面は出さず、通常モックをそのまま表示 |

ウェルカム画面では「3分デモを開始」または「自由に画面を見る」を選択する。
いずれを選んでも `teishutsu_demo_seen_v1` は表示済みとして記録される。

### 開始時の状態初期化

「3分デモを開始」を押すと `Store.reset()` 相当を実行し、必ず同じ状態から始まる。
加えて、デモを2分30秒〜4分に収めるため、開始時のみ会社の「労災保険資料」を
準備済み扱いにする（山田太郎の資格証不足のみを体験してもらうための調整）。
`js/data.js` の `SEED_DATA`（通常モードの初期データ）自体は変更しない。

## 4. ステップ構成（全6ステップ）

| STEP | 名称 | 開始条件 | 完了条件 |
|---|---|---|---|
| 1 | 現場の不足を確認 | デモ開始直後（ダッシュボード） | 「○○ビル新築工事」の現場詳細で、山田太郎の「高所作業車運転技能講習」行にある「依頼」ボタン（`[data-action="open-request-modal"][data-worker="w1"][data-cert="ct2"]`）を押す。スポットライトは行全体ではなくこのボタン自体を対象とし、PC・スマホで実際に押す要素と一致させる |
| 2 | 不足資料を作業員へ依頼 | 依頼モーダルが開いた状態 | 「依頼を送信」を押す |
| 3 | 作業員側の提出を体験 | 依頼送信完了画面 | 「作業員側の提出を体験」→ サンプル資格証で提出 →「管理画面へ戻る」 |
| 4 | 届いた資格証を確認・登録 | 通知ベルに新着が届いた状態 | 通知を開き、内容を確認して「登録」を押す |
| 5 | 不足解消を確認 | 登録直後、現場詳細に自動遷移 | 準備率100%を確認し、「提出資料を準備する」を押す |
| 6 | 提出資料を準備 | 提出資料生成画面 | 「提出資料を準備する」を押し、疑似生成が完了する |

各ステップの状態遷移は `js/demo.js` の `Demo.onRender()` / `Demo.onAction()` /
`Demo.onRequestSent()` / `Demo.onCertRegistered()` / `Demo.closeWorkerFrame()` が担う。
既存の業務ロジック（`sendMissingRequests` / `addWorkerCert` / `generateSubmission` 等）は
一切変更せず、その実行結果を監視してデモの進行に反映するだけにとどめている。

## 5. UI要素

- **進捗バー**（画面下部に固定）：「3分デモ」ラベル、6つのドット、`STEP n / 6`、
  現在のステップの短い一言、「デモを終了」ボタン。
- **スポットライト**：対象要素を枠線＋淡い背景でハイライトし、周囲を暗くする。
  色だけに依存しないよう、枠・背景・吹き出しの文言を併用する。
- **吹き出し**：対象要素の上または下に表示。内容は「説明」と「次に何を押すか」のみで、
  5行以上の長文・スライド送り的な「次へ」ボタンは使わない。
- **作業員側の提出体験**：`mobile-submit.html` を `iframe` で呼び出す。
  PCは375px幅のスマホフレーム、スマホは全画面表示。
- **完了モーダル**：達成リストの振り返り、利用意向・価格受容度の質問、
  「もう一度体験する」「自由に画面を見る」。

## 6. 完了条件

以下をすべて満たした時点で「デモ完了」とする（`Demo.complete()`）。

1. STEP1〜6のすべてを実際の画面操作で完了している（スキップ・自動遷移のみでの完了はない）
2. 提出資料の疑似生成が完了している（`site.submissionGenerated === true`）
3. `demo_completed` イベントが記録される

**ガイドUIを終了してから完了モーダルを表示する。** `Demo.complete()` はまず `Demo.endGuide()` を
呼び、スポットライト・進捗バーの非表示、`body.demo-active` の解除、作業員フレームのクローズを
行ったうえで完了モーダルを開く。これにより完了モーダルが進捗バー等の下に隠れることはなく、
モーダルが開いた時点で裏側は既に通常モード相当の状態になっている。

完了モーダルには「もう一度体験する」「自由に画面を見る」を**最初から**表示する。
「利用意向」「価格受容度」の2問への回答は任意であり、回答しなくてもこの2つのボタンで
いつでも抜けられる（右上の×で閉じた場合も同様に、抜けた時点で通常モードとして自由に
操作できる状態になっている。閉じる前に既に `Demo.endGuide()` 済みのため、`Demo.active` が
中途半端な状態で残ることはない）。回答した場合のみ `teishutsu_demo_feedback_v1` に保存される
（未回答でも、それまでのイベントログは `teishutsu_demo_events_v1` に残る）。

## 7. イベント一覧

`recordDemoEvent(event, meta)` が `teishutsu_demo_events_v1` に `{ event, timestamp, ...meta }` を追記する。

| イベント名 | 発生タイミング |
|---|---|
| `demo_started` | 「3分デモを開始」／「もう一度体験する」でデモが始まった |
| `step_1_completed` | 現場詳細で、山田太郎の「高所作業車運転技能講習」の「依頼」ボタンを押した |
| `step_2_completed` | 不足資料の依頼を送信した |
| `worker_submission_completed` | 作業員側の画面から資格証を提出した |
| `step_3_completed` | 作業員側の提出体験を終えて管理画面へ戻った |
| `cert_registered` | 資格証を登録した |
| `step_5_completed` | 不足解消を確認し、提出資料生成画面へ進んだ |
| `submission_generated` | 提出資料の疑似生成が完了した |
| `demo_completed` | 完了モーダルが表示された（`demoDurationSeconds` を含む） |
| `use_intent_answered` | 利用意向の質問に回答した（`value` を含む） |
| `price_intent_answered` | 価格受容度の質問に回答した（`value` を含む） |
| `demo_exited` | 「デモを終了」で途中離脱した（`atStep` を含む） |

## 8. 価格検証の質問

デモ完了直後、完了モーダルの下部に表示する。

**質問1**：このシステムを実際の業務で使ってみたいと思いましたか？
`ぜひ使いたい` / `料金次第で使いたい` / `今は必要ない`

回答後、Standardプラン（月額4,980円・税別、作業員50名まで、現場数無制限、
資格証自動読み取り、不足資料依頼、提出準備チェック、提出資料生成）を表示。

**質問2**：月額4,980円なら利用したいと思いますか？
`この価格なら利用したい` / `無料トライアルなら試したい` / `少し高い` / `必要ない`

## 9. 将来のAnalytics接続ポイント

現時点では外部Analytics（GA等）は導入していない。将来接続する際は、
`js/demo.js` の `recordDemoEvent()` 内（`localStorage` への書き込み箇所）に
外部送信呼び出しを追加するだけで済むよう、イベント名・メタデータの形は
汎用的な `{ event, timestamp, ...meta }` に統一している。
`saveDemoFeedback()` についても同様に、完了時点の1回だけ呼ばれる単一の関数に
集約している。

## 10. 変更しないもの

以下は既存の通常モックと完全に共通で、デモ機能の追加によって変更していない。

- ルーティング（ハッシュベースSPAルーター）
- `localStorage` のキー構成・`schemaVersion`
- 資格期限ロジック（`deadlineType` ごとの判定）
- 提出判定・不足資料依頼・資格登録・提出資料生成の各ビジネスロジック
- 作業員側スマホ提出（`mobile-submit.html`）の基本UI・ロジック
- 既存READMEのシナリオA〜D
